{% extends 'base.html.twig' %}

{% block title %}Hello TaskController!{% endblock %}

{% block body %}
{{ form_errors(form) }}
{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.content) }}
    <div class="my-3">
        <button type="submit" class="btn btn-primary">Valider ma tâche</button>
    </div>
{{ form_end(form) }}

{% endblock %}
