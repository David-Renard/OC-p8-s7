{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur Todo List
{% endblock %}

{% block body %}
    <h1 class="text-center h3 text-primary">Bienvenue sur Todo List, l'application vous permettant de gérer l'ensemble de vos tâches sans effort !</h1>
    <div class="row">
        <a href="{{ path('task_create') }}" class="btn btn-success mb-2">Créer une nouvelle tâche</a>
        <a href="{{ path('task_list') }}" class="btn btn-info mb-2">Consulter la liste des tâches à faire</a>
        <a href="{{ path('task_closed') }}" class="btn btn-secondary mb-2">Consulter la liste des tâches terminées</a>
    </div>
{% endblock %}
