<nav class="d-flex flex-row justify-content-between navbar navbar-light navbar-fixed-top" style="background-color: #e3f2fd;" role="navigation">
    <div class="ms-5">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path('homepage') }}">To Do List app<img src="{{ asset('build/images/Logo_OpenClassrooms.png') }}" alt="OpenClassrooms" /></a>
        </div>
    </div>

    <div class="me-5 w-10" id="navbarNav">
        <ul class="d-flex flex-row justify-content-around navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('homepage') }}" title="page d'accueil"><i class="bi bi-house-door fs-4"></i></a>
            </li>
            {% if is_granted('ROLE_USER') == false %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}" title="se connecter"><i class="bi bi-person-up fs-4"></i></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href=" {{ path('user_create') }}" title="s'inscrire"><i class="bi bi-window-plus fs-4"></i></a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('user_edit', {'id' : app.user.id}) }}" title="modifier mon profil"><i class="bi bi-person-lines-fill fs-4"></i></a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('task_create') }}" class="nav-link" title="créer une tâche"><i class="bi bi-clipboard-plus fs-4"></i></a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_logout') }}" class="nav-link" title="se déconnecter"><i class="bi bi-box-arrow-right fs-4"></i></a>
                </li>
                {% if is_granted('ROLE_ADMIN') == true %}
                    <li class="nav-item">
                        <a class="nav-link" href=" {{ path('admin_user_list') }}" title="administration"><i class="bi bi-award-fill fs-4"></i></a>
                    </li>
                {% endif %}
            {% endif %}
        </ul>
    </div>
</nav>